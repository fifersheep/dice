name: dice
description: A game of liar's dice.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  auto_route: ^2.2.0
  bloc: ^7.2.0
  cloud_firestore: ^2.2.0
  cloud_functions: "^1.1.2"
  collection: ^1.15.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.0
  shared_preferences: ^2.0.6
  stream_transform: ^2.0.0
  vector_math: ^2.1.0

dev_dependencies:
  auto_route_generator: ^2.1.0
  build_runner: null
  flutter_test:
    sdk: flutter
  freezed: ^0.14.2

flutter:
  uses-material-design: true
